import React from "react";
import Table from "./Table";
import Form from "./Form";
// import { MusicPlayerContext } from "./useContext";

const Data_statistics = () => {
  // const [data, setData, handleChange] = useContext(MusicPlayerContext);
  return (
    <div>
      <Form />
      <Table />
    </div>
  );
};

export default Data_statistics;
